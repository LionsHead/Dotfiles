# EditorConfig is awesome: http://EditorConfig.org

# Visual Studio Code does not support EditorConfig out of the box - install extension
# Supported Properties:
#   indent_style
#   indent_size
#   tab_width
#   end_of_line (on save)
#   insert_final_newline (on save)
#   trim_trailing_whitespace (on save)

# top-most EditorConfig file
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
